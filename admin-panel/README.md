# YESS Admin Panel

Админ-панель для системы лояльности YESS Go.

## 🔗 Подключение к бэкенду

Бэкенд расположен в репозитории: [Yess-go-v2](https://github.com/inspireVictim/Yess-go-v2.git)

### API Endpoint

По умолчанию админ-панель подключается к API через прокси Vite:
- **Development**: `/api/v1` → проксируется на `https://api.yessgo.org`
- **Production**: можно задать через переменную окружения `VITE_API_URL`

### Настройка API URL

Для продакшена создайте файл `.env` в корне `admin-panel/`:

```env
VITE_API_URL=https://api.yessgo.org
```

Или для локального бэкенда:

```env
VITE_API_URL=http://localhost:8080
```

## 🔐 Дефолтный администратор

Для быстрого доступа создан дефолтный администратор:

- **Логин**: `admin2025`
- **Пароль**: `admin2025_yessgo`

Этот администратор:
- ✅ Всегда работает с реальными данными через API
- ✅ Все операции (CRUD) выполняются в реальной базе данных
- ✅ Если API временно недоступен, вход разрешается, но данные загружаются из реального API

## 🚀 Запуск

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Админ-панель будет доступна по адресу: `http://localhost:3001`

### Сборка для продакшена

```bash
npm run build
```

## 📁 Структура проекта

```
admin-panel/
├── src/
│   ├── components/      # React компоненты
│   ├── pages/          # Страницы админ-панели
│   ├── services/        # API клиенты
│   │   ├── adminApi.ts # Основной API клиент
│   │   └── api.ts      # Агрегатор API
│   ├── hooks/          # React хуки
│   ├── store/          # Zustand store
│   └── types/          # TypeScript типы
├── public/             # Статические файлы
│   └── favicon.png     # Иконка компании
└── vite.config.ts      # Конфигурация Vite
```

## 🔧 API Endpoints

Админ-панель использует следующие эндпоинты бэкенда:

- **Аутентификация**: `/api/v1/admin/auth/login`
- **Пользователи**: `/api/v1/admin/users`
- **Партнеры**: `/api/v1/admin/partners`
- **Товары**: `/api/v1/partners/{id}/products`
- **Промоакции**: `/api/v1/admin/promotions`
- **Транзакции**: `/api/v1/admin/transactions`
- **Уведомления**: `/api/v1/admin/notifications`
- **Аудит**: `/api/v1/admin/audit/logs`
- **Настройки**: `/api/v1/admin/settings`

## 📝 Основные функции

- ✅ Управление пользователями
- ✅ Управление партнерами и их товарами
- ✅ Управление промоакциями
- ✅ Просмотр транзакций
- ✅ Отправка уведомлений
- ✅ Аудит логов
- ✅ Настройки системы
- ✅ Импорт/экспорт данных (JSON, CSV)
- ✅ Загрузка изображений (логотипы, фото товаров)

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Ant Design** - компоненты UI
- **React Query** - управление состоянием сервера
- **Axios** - HTTP клиент
- **React Router** - маршрутизация
- **Vite** - сборщик

## 📄 Лицензия

© 2025 Yess Loyalty. Все права защищены.

