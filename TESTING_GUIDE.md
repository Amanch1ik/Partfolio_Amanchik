# 🧪 Руководство по тестированию панелей

## ✅ Статус запуска

Dev-серверы успешно запущены:

- **Admin Panel**: http://localhost:3003 ✅
- **Partner Panel**: http://localhost:3001 ✅

## 🌐 Доступ к панелям

### Admin Panel (Админ-панель)
```
URL: http://localhost:3003
Порт: 3003
Статус: Запущен (PID: 5448)
```

### Partner Panel (Партнерская панель)
```
URL: http://localhost:3001
Порт: 3001
Статус: Запущен (PID: 7036)
```

## 🔧 Проверка работы

### 1. Откройте панели в браузере

1. **Admin Panel**: 
   - Откройте http://localhost:3003
   - Должна загрузиться страница входа

2. **Partner Panel**:
   - Откройте http://localhost:3001
   - Должна загрузиться страница входа

### 2. Проверка консоли браузера

Откройте DevTools (F12) и проверьте:
- ❌ **Нет ошибок** в Console
- ✅ **Нет 404 ошибок** для ресурсов
- ✅ **Нет ошибок загрузки** модулей

### 3. Проверка интеграции с бэкендом

#### Требования:
- **Backend API** должен быть запущен на `http://localhost:8000`
- Если бэкенд не запущен, панели покажут ошибки подключения

#### Проверка API:
```bash
# Проверьте, что бэкенд запущен
curl http://localhost:8000/health
# или
curl http://localhost:8000/api/v1/health
```

#### Что проверить:
1. **Авторизация**:
   - Попробуйте войти в систему
   - Проверьте сохранение токена
   - Проверьте редирект после входа

2. **API запросы**:
   - Откройте Network tab в DevTools
   - Проверьте, что запросы идут на `/api/*`
   - Проверьте статусы ответов (200, 401, 403 и т.д.)

3. **Проксирование**:
   - Все запросы к `/api` автоматически проксируются на `http://localhost:8000`
   - Проверьте в Network tab, что запросы проходят

## 📋 Чек-лист проверки

### Admin Panel

- [ ] Страница входа загружается
- [ ] Нет ошибок в консоли браузера
- [ ] После входа перенаправляет на дашборд
- [ ] Навигация работает (все пункты меню)
- [ ] Страницы загружаются без ошибок:
  - [ ] Dashboard (Главная)
  - [ ] Users (Пользователи)
  - [ ] Partners (Партнеры)
  - [ ] Partners Map (Карта партнеров)
  - [ ] Transactions (Транзакции)
  - [ ] Notifications (Уведомления)
  - [ ] Promotions (Промо-акции)
  - [ ] Stories (Истории)
  - [ ] Referrals (Рефералы)
  - [ ] Settings (Настройки)
  - [ ] Audit (Аудит)

### Partner Panel

- [ ] Страница входа загружается
- [ ] Нет ошибок в консоли браузера
- [ ] После входа перенаправляет на дашборд
- [ ] Навигация работает (все пункты меню)
- [ ] Страницы загружаются без ошибок:
  - [ ] Dashboard (Главная)
  - [ ] Profile (Профиль)
  - [ ] Locations (Локации)
  - [ ] Promotions (Промо-акции)
  - [ ] Transactions (Транзакции)
  - [ ] Employees (Сотрудники)
  - [ ] Settings (Настройки)

## 🐛 Решение проблем

### Серверы не запускаются

```powershell
# Проверьте, заняты ли порты
netstat -ano | findstr ":3001 :3003"

# Если порты заняты, остановите процессы или измените порты в vite.config.ts
```

### Ошибки подключения к API

1. **Проверьте, запущен ли бэкенд**:
   ```bash
   curl http://localhost:8000/health
   ```

2. **Проверьте переменные окружения**:
   - Убедитесь, что в `.env` файле указан правильный `VITE_API_URL`
   - По умолчанию: `VITE_API_URL=http://localhost:8000`

3. **Проверьте CORS настройки бэкенда**:
   - Бэкенд должен разрешать запросы с `http://localhost:3001` и `http://localhost:3003`

### Ошибки загрузки модулей

1. **Переустановите зависимости**:
   ```bash
   cd admin-panel
   rm -rf node_modules package-lock.json
   npm install
   
   cd ../partner-panel
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **Очистите кеш Vite**:
   ```bash
   rm -rf node_modules/.vite
   ```

## 📊 Мониторинг

### Проверка процессов

```powershell
# Посмотреть процессы Node.js
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# Остановить все Node.js процессы (если нужно)
Get-Process | Where-Object {$_.ProcessName -like "*node*"} | Stop-Process -Force
```

### Логи

Логи выводятся в окнах PowerShell, которые были запущены для dev-серверов.

## 🔄 Остановка серверов

1. Закройте окна PowerShell, где запущены серверы
2. Или используйте `Ctrl+C` в окнах терминала
3. Или остановите процессы по PID:
   ```powershell
   Stop-Process -Id 5448  # Admin Panel
   Stop-Process -Id 7036  # Partner Panel
   ```

## ✅ Успешная проверка

Если все проверки пройдены успешно:
- ✅ Обе панели доступны в браузере
- ✅ Нет ошибок в консоли
- ✅ Навигация работает
- ✅ API запросы проходят (если бэкенд запущен)
- ✅ Страницы загружаются корректно

**Панели готовы к использованию!** 🎉

