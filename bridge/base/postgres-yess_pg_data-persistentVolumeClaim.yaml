#! postgres-yess_pg_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgres-yess-pg-data
    namespace: yessbackend-master
    labels:
        com.docker.compose.service: postgres
        com.docker.compose.volume: yess_pg_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
